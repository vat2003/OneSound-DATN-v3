import { Observable } from 'rxjs';
import { ShareButtonFuncArgs } from './share.models';
/**
 * Deep merge two objects.
 */
export declare function mergeDeep(target: any, ...sources: any[]): any;
/** Returns a valid URL or falls back to current URL */
export declare function getValidUrl(url: string, fallbackUrl: string): string;
export declare function printPage(): Observable<void>;
export declare function copyToClipboard({ params, data, clipboard, updater }: ShareButtonFuncArgs<CopyToClipboardDataArgs>): Observable<void>;
interface CopyToClipboardDataArgs {
    delay: number;
    text: string;
    icon: string[];
    successText: string;
    successIcon: string[];
}
export {};
