import { BehaviorSubject } from 'rxjs';
import { IShareButton, ShareButtonsConfig } from './share.models';
import * as i0 from "@angular/core";
export declare class ShareService {
    private _document;
    /** Global config that applies on all share buttons in the app */
    config: ShareButtonsConfig;
    /** Stream that emits when config changes */
    config$: BehaviorSubject<ShareButtonsConfig>;
    constructor(config: ShareButtonsConfig, _document: any);
    /**
     * Share buttons properties, used to get the text, color and icon of each button.
     */
    get prop(): import("./share.models").IShareButtons;
    get windowSize(): string;
    setConfig(config: ShareButtonsConfig): void;
    addButton(name: string, props: IShareButton): void;
    static ɵfac: i0.ɵɵFactoryDeclaration<ShareService, [{ optional: true; }, null]>;
    static ɵprov: i0.ɵɵInjectableDeclaration<ShareService>;
}
